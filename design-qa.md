# 分身资料与相机重拍 Design QA

- Reference: `design-qa-my-ai-simplified.jpg`（上一版“我的分身”页面）与用户提供的竞品相机界面。
- Current implementation: `design-qa-profile-hero-summary.jpg`、`design-qa-profile-detail.jpg`、`design-qa-camera-retake.jpg`。
- Viewport: 390 × 844 CSS px；同时由响应式回归覆盖 375、390、430 px 和桌面窄高视口。

## Findings

- 无 P0/P1/P2 问题。
- 分身主图保持 1:1，脸型名称和说明叠放在底部渐变区，文字对比度足够且没有截断。
- 页面已删除独立脸型区和主图删除按钮；“我的分身库”紧接主图并位于推荐发型之前。
- 分身详情采用底部弹窗，展示头像、名称、脸型和创建时间。当前分身的主按钮显示“当前使用中”并禁用；其他分身显示“设为当前分身”。
- 删除入口收进详情弹窗，并继续使用二次确认，减少主页面上的破坏性操作。
- 点击“重拍”会进入明确的重拍状态：恢复虚线脸部引导、角度文案和圆形快门；拍下后返回该照片的高亮预览态。
- 未选中的已拍缩略图继续使用黑色半透明遮罩；当前照片保留彩色和白色描边。

## Interaction checks

- 点击当前分身头像可打开详情，创建时间可见，“当前使用中”不可点击。
- 点击非当前分身头像可打开详情，“设为当前分身”可点击并立即更新当前分身。
- 从详情删除会打开确认弹窗；取消保留分身，确认删除后自动选择剩余分身。
- 三张照片拍齐后选择任意缩略图，点击“重拍”进入对应角度；再次点击快门替换该角度，其余两张照片保持不变。
- 相机完成按钮仍占用原圆形快门位置，不出现额外长条确认页。

## Validation

- TypeScript/Vite production build passed。
- 27 个 Playwright 回归测试全部通过；新增断言覆盖主图脸型文案、详情弹窗、创建时间、当前按钮禁用、切换、删除确认和重拍状态。

final result: passed
